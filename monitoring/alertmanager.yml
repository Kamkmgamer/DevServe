route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h
  receiver: 'devserve-default'

receivers:
  - name: 'devserve-default'
    # Example email receiver (fill in real SMTP creds via env or secrets)
    # email_configs:
    #   - to: 'ops@example.com'
    #     from: 'alerts@example.com'
    #     smarthost: 'smtp.example.com:587'
    #     auth_username: 'alerts@example.com'
    #     auth_password: 'REPLACE_ME'

    # Example Slack (requires incoming webhook URL)
    # slack_configs:
    #   - send_resolved: true
    #     api_url: 'https://hooks.slack.com/services/REPLACE/ME'
    #     channel: '#alerts'
    #     text: |
    #       *{{ .CommonLabels.alertname }}* ({{ .Status }})\n
    # For now, no-op receiver until credentials are provided

inhibit_rules:
  - source_matchers: [ severity = 'critical' ]
    target_matchers: [ severity = 'warning' ]
    equal: ['alertname']
